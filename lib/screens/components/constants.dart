import 'package:flutter/material.dart';

const primaryColor = Color(0xFFFC107);
const secondaryColor = Color(0xFF242430);
const animationColor = Color.fromRGBO(225, 74, 57, 1);
const darkColor = Color(0xFF191923);
const bodyTextColor = Color(0xFF8B8BBD);
const bgColor = Color(0xFF1E1E28);

const double defaultPadding = 18;
const defaultDuration = Duration(seconds: 1);
const maxWidth = 1440.0;
const textStyleDefault = TextStyle(color: Colors.white);
const defaultSizedBox = SizedBox(
  height: defaultPadding,
);
